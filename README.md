Document Extraction System
==========================

Layout-first, hybrid extraction pipeline for structured data from PDFs.

Install (editable):

```bash
pip install -e .
```

Requires Python >= 3.10.

This repo will implement the pipeline described in `context.md`.

